{
  "name": "@hamcq/flarum-ext-auth-phone",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "flarum-webpack-config": "^0.1.0-beta.10",
    "webpack": "^4.26.0"
  },
  "scripts": {
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --mode development --watch",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --mode production"
  },
  "devDependencies": {
    "webpack-cli": "^4.10.0"
  }
}
